{
  "name": "rmm-subgraph",
  "version": "1.0.0",
  "description": "Subgraph pour capturer les événements RMM et les transferts de SupplyTokens",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/",
    "create-local": "graph create --node http://localhost:8020/ rmm-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ rmm-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rmm-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.45.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
} 